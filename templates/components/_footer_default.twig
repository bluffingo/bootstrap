<footer class="p-3">
    <div class="container">
        <div class="card">
            <div class="card-body">
                <h4 class="my-0">OpenSB {{ opensb_version }}</h4>
                <p class="my-0">© 2021-{{ 'now' | date('Y') }} OpenSB contributors</p>
                <p class="my-0">
                    {% for link in footer_links() %}
                        <a href="{{ link.url }}" class="me-1">{{ link.name }}</a>
                    {% endfor %}
                </p>
            </div>
        </div>
    </div>
</footer>