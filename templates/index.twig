{% extends "_layout.twig" %}

{% block title %}Home{% endblock %}
	{% block content %}
        {% if current_theme == "alpha2" %}
            {{ include('/subpages/_index_alpha2.twig') }}
        {% else %}
            {{ include('/subpages/_index.twig') }}
        {% endif %}
    {% endblock %}
